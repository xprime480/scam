#include "form/SyntaxUtils.hpp"

bool scam::installSyntax(Env * env,
                         ScamEngine * engine,
                         ScamValue symbol,
                         ScamValue rules)
{
    return true;
}
