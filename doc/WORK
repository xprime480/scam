Work stack:

* Treat all command line arguments that don't start with '-' as the name
  of a file to load

* Add -t flag to report status of last command as program exit code
  ** Integer reported directly
  ** #f => 1
  ** anything else => 0

* Add ability to scam to run scripts in test mode

* Implement scam based testing framework

* Move as many unit tests to integration tests as possible

* Rewrite all integration tests in scam based testing framework

* Eliminate current C++ integration test code

* Implement define-syntax

* Implement proper library syntax

* Rewrite / eliminate prelude.scm

* Implement all of r7rs

* Eliminate ArgParser concept

* Eliminate Validator concept


