Match non-empty pattern to empty data

;[skip]

[name]
Match Var False

[input]

(define match-data (match '(:X) '()))
(if (dict? match-data)
    match-data
    (car (cdr match-data)))

[result]
[keep 1]
"Pattern: (:X) does not conform to data: ()"
