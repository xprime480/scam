Match dictionary pattern to conforming dictionary data

;[skip]

[name]
Match Dictionary

[input]

(define the-pattern { :first :name :second :age })
(define the-data { :second 58 :first "Mike" })
(match the-pattern the-data)

[result]
[keep 1]
{ :name Mike :age 58 }
