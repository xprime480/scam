Read dictionary literals

;[skip] uncomment to skip this test

[name]
Dictionary Operations

[input]
(define aDict {})
(aDict :put :key 22)
(aDict :put fun "hello")
(aDict :put :key -1.5)
(aDict :get :key)
(begin
  (aDict :remove :key)
  (aDict :length))
aDict

[result]
[keep 3]
-1.5
1
{ fun hello }
