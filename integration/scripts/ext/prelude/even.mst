Even? function

;[skip] uncomment to skip this test

[name]
Even?

[input]

(load "lib/prelude.scm")

(list (even? -2)
      (even? 0)
      (even? 2)
      (even? 12398234))

(list (even? -3)
      (even? 3)
      (even? 82383))

(list (even? 2.0)
      (even? #t)
      (even? "Silly, strings don't have parity"))

[result]
[keep 3]
(#t #t #t #t)
(#f #f #f)
(#t #f #f)
