Odd? function

;[skip] uncomment to skip this test

[name]
Odd?

[input]

(load "lib/prelude.scm")

(list (odd? -3)
      (odd? 3)
      (odd? 82383))

(list (odd? -2)
      (odd? 0)
      (odd? 2)
      (odd? 12398234))

(list (odd? 3.0)
      (odd? #t)
      (odd? "Silly, strings don't have parity"))

[result]
[keep 3]
(#t #t #t)
(#f #f #f #f)
(#t #f #f)
