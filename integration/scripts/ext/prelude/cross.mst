Cross function

;[skip] uncomment to skip this test

[name]
Cross

[input]

(load "lib/prelude.scm")

(cross cons '() '())
(cross cons '() '((1) (2)))
(cross (lambda (a b) (+ a b)) '(1 2 3) '())
(let ((a '(1 2 3)))
  (cross + a a))

[result]
[keep 4]
()
()
()
(2 3 4 3 4 5 4 5 6)
