Power-set function

;[skip] uncomment to skip this test

[name]
Power-Set

[input]

(load "lib/prelude.scm")

(power-set '())
(power-set '(1))
(let ((pset (power-set '(1 2))))
  (list
   (length pset)
   (member? '() pset)
   (member? '(1) pset)
   (member? '(2) pset)
   (member? '(1 2) pset)))

[result]
[keep 3]
(())
(() (1))
(4 #t #t #t #t)
