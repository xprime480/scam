Call own function via self in parent

;[skip] uncomment to skip this test

[name]
Parent Calls Self Function

[input]

(let* ((Parent
        (make-class
         Root
         ()
         (jarthur () (* (self doodle) 17))
         (doodle () 1)))
       (Trivial
        (make-class
         Parent
         ()
         (get () (parent jarthur))
         (doodle () 2)))
       (obj (Trivial)))
  (obj get))

[result]
34
