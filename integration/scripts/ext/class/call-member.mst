Call a member function with self

;[skip] uncomment to skip this test

[name]
Class Without Init

[input]

(let* ((Trivial
        (make-class
         Root
         ()
         (get () (self jarthur 1))
         (jarthur (n) (* n 17))))
       (obj (Trivial)))
  (list
   (obj get)
   (obj jarthur 3)))

[result]
(17 51)
