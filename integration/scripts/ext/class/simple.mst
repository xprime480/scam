Class which simply stores a constant

;[skip] uncomment to skip this test

[name]
Simple Constant Class

[input]

(let* ((SimpleConstant
        (make-class
         Root
         (val)
         (init (v) (assign! val v))
         (get () val)))
       (k 21)
       (answer (SimpleConstant (* k 2))))
  (answer get))

[result]
42
