Class with runtime initialization error

;[skip] uncomment to skip this test

[name]
Class Init Error

[input]

(let* ((Trivial (make-class
                 Root
                 (n)
                 (init (v) (set! n v)))))
  (Trivial (/ 1 0)))

[result]
Division By Zero
