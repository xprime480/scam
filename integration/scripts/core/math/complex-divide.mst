Test operator / on complex numbers

;[skip] uncomment to skip this test

[name]
Complex Divide

[input]

(define check
  (lambda (val)
    (list val
          (complex? val)
          (real? val)
          (rational? val)
          (integer? val)
          (exact? val))))

(check (/ 1+i 1-i))
(check (/ 1+i 1.0-i))
(check (/ 1+i 2+i))
(check (/ 1+i 1+i))

[result]
[keep 4]
(+i #t #f #f #f #t)
(+i #t #f #f #f #f)
(3/5+1/5i #t #f #f #f #t)
(1 #t #t #t #t #t)
