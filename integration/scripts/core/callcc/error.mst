Call/CC invoked in various incorrect ways

;[skip] uncomment to skip this test

[name]
Bad Continuation Invocation

[input]

(load "lib/test/test-handler.scm")

(call/cc 2)
(test-err-cat (+ 4 (call/cc (lambda (k) (k)))))
(test-err-cat (+ 4 (call/cc (lambda (k) (k 1 2)))))

[result]
[keep 3]
call/cc: form '2' cannot be applied
:args
:args
