Take the length of some lists

;[skip] uncomment to skip this test

[name]
List Length

[input]

(load "lib/listops.scm")

(length)
(length () ())
(length '(a . b))
(length ())
(length '(1 (sublist counts as 1) 3))
(exact? (length '()))

[result]
[keep 6]
Too few parameters (0 of 1)
Too many parameters (2 of 1)
length: expected a list, got: (a . b)
0
3
#t
