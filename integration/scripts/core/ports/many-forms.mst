String Port usage

;[skip] uncomment to skip this test

[name]
String Port

[input]

(define text "1 2 3 4")

(let ((data (open-input-string text)))
  (list
   (read data)
   (read data)
   (read data)
   (read data)))

[result]
(1 2 3 4)
