Test access to components of pair values

;[skip] uncomment to skip this test

[name]
Car / Cdr

[input]

(load "lib/test/test-handler.scm")

(define pair (cons 1 2))
(define not-a-pair 2)
(define list-val (list 1 2 3))

(test-err-cat (car not-a-pair))
(test-err-cat (cdr not-a-pair))

(car pair)
(cdr pair)

(car list-val)
(cdr list-val)
(car (cdr list-val))


[result]
[keep 7]
:args
:args
1
2
1
(2 3)
2
