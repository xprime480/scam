Malformed lambda expressions

;[skip] uncomment to skip this test

[name]
Malformed Lambda Expressions

[input]

(load "lib/test/test-handler.scm")

(test-err-cat (lambda))
(test-err-cat (lambda 2 (+ 2 2)))
(test-err-cat (lambda (:keyword) (+ 2 2)))
(test-err-cat (lambda (x x) (* x 2)))
(test-err-cat (lambda (x . x) (* x 2)))
(test-err-cat (lambda (ok . :keyword) (+ 2 2)))

[result]
[keep 6]
:args
:args
:args
:args
:args
:args
