Closure with dotted parameter list

;[skip] uncomment to skip this test

[name]
Lambda With Dotted Formals

[input]

(let ((f (lambda (x . y)
           y)))
  (list
   (f 1)
   (f 1 2)
   (f 1 2 (+ 2 2) #t)))



[result]
(() (2) (2 4 #t))
