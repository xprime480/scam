Read bad input or bad syntax

;[skip] uncomment to skip this test

[name]
Read Errors

[input]

(load "lib/test/test-handler.scm")

(test-err-cat (read))
(test-err-cat (read (open-input-string "") :nope))
(test-err-cat (read 2))
(test-err-cat (read (open-input-string "(2 3 4")))
(test-err-cat (read (open-input-string ")")))


[result]
[keep 5]
:args
:args
:args
:read
:read
