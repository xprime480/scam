End-of-File objects

;[skip] uncomment to skip this test

[name]
EOF

[input]

(load "lib/test/test-handler.scm")

(test-err-cat (eof-object 2))
(eof-object)
(eof-object? (eof-object))

[result]
[keep 3]
:args
eof
#t
