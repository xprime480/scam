Test the eof-object? function

;[skip]

[name]
Type Checker of EOF

[input]

(load "lib/test/test-handler.scm")

(test-err-cat (eof-object?))
(test-err-cat (eof-object? '(a . b) (list 1 2 3)))

(list
 (eof-object? 2)
 (eof-object? (eof-object)))

[result]
[keep 3]
:args
:args
(#f #t)
