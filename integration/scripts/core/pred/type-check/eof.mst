Test the eof-object? function

;[skip]

[name]
Type Checker of EOF

[input]

(eof-object?)
(eof-object? '(a . b) (list 1 2 3))

(list
 (eof-object? 2)
 (eof-object? (eof-object)))

[result]
[keep 3]
eof-object?: parameter 1 missing
eof-object?: Expected 1 values, got 2
(#f #t)
