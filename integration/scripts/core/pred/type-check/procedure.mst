Test the procedure? function

;[skip]

[name]
Type Checker of Procedures

[input]

(load "lib/test/test-handler.scm")

(test-err-cat (procedure?))
(test-err-cat (procedure? car cdr))

(list
 (procedure? car)
 (procedure? 'car)
 (procedure? (lambda (x) (* x x)))
 (procedure? '(lambda (x) (* x x)))
 (call/cc procedure?))

[result]
[keep 3]
:args
:args
(#t #f #t #f #t)
