Test the list? function

;[skip]

[name]
Type Checker of Lists

[input]

(load "lib/test/test-handler.scm")

(test-err-cat (list?))
(test-err-cat (list? '(a . b) (list 1 2 3)))

(list
 (list? (cons 1 2))
 (list? (list 1 2 3))
 (list? 2)
 (list? '())
 (list? '(1 2 3 4 5 6 . 7)))

[result]
[keep 3]
:args
:args
(#f #t #f #t #f)
