Test the list? function

;[skip]

[name]
Type Checker of Lists

[input]

(list?)
(list? '(a . b) (list 1 2 3))

(list
 (list? (cons 1 2))
 (list? (list 1 2 3))
 (list? 2)
 (list? '())
 (list? '(1 2 3 4 5 6 . 7)))

[result]
[keep 3]
list?: parameter 1 missing
list?: 1 parameter needed, 2 given
(#f #t #f #t #f)
