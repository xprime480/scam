Test the pair? function

;[skip]

[name]
Type Checker of Pairs

[input]

(load "lib/test/test-handler.scm")

(test-err-cat (pair?))
(test-err-cat (pair? '(a . b) (list 1 2 3)))

(list
 (pair? (cons 1 2))
 (pair? (list 1 2 3))
 (pair? 2)
 (pair? '()))

[result]
[keep 3]
:args
:args
(#t #t #f #f)
