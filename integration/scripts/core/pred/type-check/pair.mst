Test the pair? function

;[skip]

[name]
Type Checker of Pairs

[input]

(pair?)
(pair? '(a . b) (list 1 2 3))

(list
 (pair? (cons 1 2))
 (pair? (list 1 2 3))
 (pair? 2)
 (pair? '()))

[result]
[keep 3]
pair?: parameter 1 missing
pair?: Expected 1 values, got 2
(#t #t #f #f)
