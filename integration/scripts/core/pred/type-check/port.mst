Test the port? function

;[skip]

[name]
Type Checker of Ports

[input]

(load "lib/test/test-handler.scm")

(test-err-cat (port?))
(test-err-cat (port? '(a . b) (list 1 2 3)))

(list
 (port? 2)
 (port? (open-input-string "")))

[result]
[keep 3]
:args
:args
(#f #t)
