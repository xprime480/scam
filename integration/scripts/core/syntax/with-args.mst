define-syntax operates like a macro in simple cases

;[skip]

[name]
Define Syntax With Arguments

[input]

(let ()
  (define-syntax sum-plus-one
    (syntax-rules ()
      ((sum-plus-one a b)
       (+ a b 1))))
  (sum-plus-one 2 (* 3 4)))

[result]
15
