define-syntax validates nested patterns

;[skip]

[name]
Define Syntax With Nested Pattern

[input]

(let ()
  (define-syntax nest-test
    (syntax-rules ()
      ((nest-test (a (b c)))
       b)))
  (nest-test (1 (2 3))))

[result]
2

