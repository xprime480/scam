Make some strings

;[skip] uncomment to skip this test

[name]
Make some strings

[input]

(load "lib/test/test-handler.scm")

(test-err-cat (make-string -5 #\*))
(test-err-cat (make-string 335 3))

(make-string 0)
(make-string 0 #\x)
(make-string 3)
(make-string 2 #\i)

(test-err-cat (string 1 2 3))

(string)
(string #\1 #\x #\*)

[result]
[keep 9]
:args
:args
""
""
"   "
"ii"
:args
""
"1x*"
