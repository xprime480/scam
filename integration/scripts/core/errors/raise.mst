Raise errors of different objects

;[skip] uncomment to skip this test

[name]
Raise

[input]

(load "lib/test/test-handler.scm")

(test-err-cat (raise))
(test-err-cat (raise 1 2 3))
(raise "bagels are not doughnuts")
(raise 2)

[result]
[keep 4]
:args
:args
"bagels are not doughnuts"
2
