Test operator % on rationals

;[skip] uncomment to skip this test

[name]
Rational Modulus

[input]

(define check
  (lambda (val)
    (list val
          (rational? val)
          (integer? val)
          (exact? val))))

(check (% 12.5 2.5))
(check (% 12.75 0.5))
(check (% 5/3 2/3))

[result]
[keep 3]
(0 #t #t #f)
(1/4 #t #f #f)
(1/3 #t #f #t)

