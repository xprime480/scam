Test operator / on integers

;[skip] uncomment to skip this test

[name]
Integer Divide

[input]

(define check
  (lambda (val)
    (list val
          (rational? val)
          (integer? val)
          (exact? val))))

(check (/))
(check (/ 2))
(check (/ 7 2))
(check (/ 4 2))
(check (/ 2 2 -3.0))

[result]
[keep 5]
(1 #t #t #t)
(1/2 #t #f #t)
(7/2 #t #f #t)
(2 #t #t #t)
(-1/3 #t #f #f)
