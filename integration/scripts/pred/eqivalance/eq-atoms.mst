Test the eq? function on atoms

;[skip]

[name]
Eq for atoms

[input]

(list (eq? #t #t)
      (eq? #f #f)
      (eq? #t #f))

(list (eq? 'sym1 'sym1)
      (eq? 'sym1 'sym2))

(list (eq? :keyword1 :keyword1)
      (eq? :keyword1 :keyword2))

(list (eq? #\a #\a)
      (eq? #\a #\b))

(list (eq? '() '())
      (eq? '() 2))

[result]
(#t #t #f)
(#t #f)
(#t #f)
(#t #f)
(#t #f)
