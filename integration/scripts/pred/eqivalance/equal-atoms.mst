Test the equal? function on atoms

;[skip]

[name]
Equal for atoms

[input]

(list (equal? #t #t)
      (equal? #f #f)
      (equal? #t #f))

(list (equal? 'sym1 'sym1)
      (equal? 'sym1 'sym2))

(list (equal? :keyword1 :keyword1)
      (equal? :keyword1 :keyword2))

(list (equal? #\a #\a)
      (equal? #\a #\b))

(list (equal? '() '())
      (equal? '() 2))

[result]
(#t #t #f)
(#t #f)
(#t #f)
(#t #f)
(#t #f)
